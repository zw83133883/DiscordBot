const fetchVideoInfo = require('./index')
const videoIds = [
  'xPU8OAjjS4k',
  'BJk6gZuPKRE',
  'b8-tXG8KrWs',
  'NU9JoFKlaZ0',
  '7QU1nvuxaMA',
  'Mlahvvymkxc',
  'uwIGZLjugKA'
]

// videoIds.reduce(
//   (prev, videoId) => prev.then(() => fetchVideoInfo(videoId)),
//   Promise.resolve()
// )

videoIds.forEach(video => {
  fetchVideoInfo(video).then(function (videoInfo) {
    console.log(videoInfo.url)
  })
})
